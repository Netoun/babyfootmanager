version: '3.3'

services:
  db:
    build: ./database
    restart: always
    ports:
      - '5432:5432'
    env_file:
      - ./database/.env
    volumes:
      - babyfootdata:/var/lib/postgresql/data

volumes:
  babyfootdata:
